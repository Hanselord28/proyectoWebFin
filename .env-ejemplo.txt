# Configuración del Servidor
PORT=3000

# Configuración de la Base de Datos (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=clinica_espana

# Seguridad (Express Session)
SESSION_SECRET=clave_super_mega_hiper_secreta_para_sesiones