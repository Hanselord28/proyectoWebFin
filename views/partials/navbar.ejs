<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-navy navbar-custom sticky-top py-3">
    <div class="container">
        <!-- Logo Principal -->
        <a class="navbar-brand d-flex align-items-center" href="/">
            <img src="/img/logo-dentales.png" alt="Clínica Dental España" height="50" style="width: auto; object-fit: contain;">
        </a>
        <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item me-3">
                    <a class="nav-link text-white fw-bold d-flex align-items-center gap-1" href="/">
                        <i class="bi bi-house-door"></i> Inicio
                    </a>
                </li>
                
                <% if (typeof isLogged !== 'undefined' && isLogged || typeof nombreUsuario !== 'undefined') { %>
                    <% if (typeof userRol !== 'undefined' && userRol === 'admin') { %>
                        <li class="nav-item me-3">
                            <a class="nav-link text-white fw-bold d-flex align-items-center gap-1" href="/admin/dashboard">
                                <i class="bi bi-shield-lock"></i> Panel Admin
                            </a>
                        </li>
                    <% } else if (typeof userRol !== 'undefined' && userRol === 'profesional') { %>
                        <li class="nav-item me-3">
                            <a class="nav-link text-white fw-bold d-flex align-items-center gap-1" href="/profesional/dashboard">
                                <i class="bi bi-heart-pulse"></i> Panel Profesional
                            </a>
                        </li>
                    <% } else { %>
                        <li class="nav-item me-3">
                            <a class="nav-link text-white fw-bold d-flex align-items-center gap-1" href="/paciente/perfil">
                                <i class="bi bi-person-circle"></i> Mi Perfil
                            </a>
                        </li>
                    <% } %>
                    <li class="nav-item me-3">
                        <a class="nav-link text-white fw-bold d-flex align-items-center gap-1" href="/auth/logout">
                            <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
                        </a>
                    </li>
                <% } else { %>
                    <li class="nav-item me-3">
                        <a class="nav-link text-white fw-bold d-flex align-items-center gap-1" href="/auth/login">
                            <i class="bi bi-box-arrow-in-right"></i> Iniciar Sesión
                        </a>
                    </li>
                <% } %>

                <li class="nav-item">
                    <a class="btn btn-gradient ms-lg-3 d-flex align-items-center gap-2" href="/citas/nueva">
                        <i class="bi bi-calendar-event"></i> Agendar Hora
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>